{
  "description": "Cost routing for a paid 3P deployment where the main thread often runs on Opus or Fable. Three single-PowerShell wirings. (1) note-model on SessionStart: records the payload's model field to %TEMP%\\eplus-model-routing\\<session_id>\\model.txt (and plugin data when set). FIELD RESULT 2026-09-03: Cowork's SessionStart payload has no model field (session_id, transcript_path, cwd, hook_event_name, source only), so on Cowork the first prompt of a session always gets the env-check note and every later prompt is detected exactly from the transcript; the model was observed reading its env line correctly plus the payload's key names as a diagnostic, and emits nothing (zero context tokens; one short launch per session open, resume, or compaction). (2) route-check on UserPromptSubmit: reads that file (falling back to the transcript tail, then to unknown) and injects a short additionalContext ONLY when the session is on Opus or Fable or the tier is unknown; the note always tells the model to confirm against the Model: line in its own env block. First injection per session is the full digest (~110 tokens), later ones a one-line reminder (~25 tokens); on Sonnet or Haiku nothing is injected. (3) spawn-gate on PreToolUse:Agent: logs every Agent spawn's subagent_type and model to routing.log under %TEMP%\\eplus-model-routing (and plugin data when set) and returns permissionDecision ask when a spawn requests Opus or Fable (field-verified 2026-09-03: the approval prompt appears in Cowork under auto mode), since the only workers here run on Sonnet and Haiku. Disable routing with EPLUS_NO_MODEL_ROUTING=1; disable the gate (logging stays) with EPLUS_ALLOW_EXPENSIVE_SPAWN=1. All scripts always exit 0; decisions travel in the JSON body. Hook form (adopted 2026-09-03 after a field A/B in the same export): every entry uses shell: powershell with a direct & script call, so the app's own PowerShell runs the script in one process (540 ms measured) instead of launching a second powershell.exe (907 ms); the single-launch form is the standard for this catalog.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "& \"${CLAUDE_PLUGIN_ROOT}\\scripts\\note-model.ps1\"",
            "shell": "powershell"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "& \"${CLAUDE_PLUGIN_ROOT}\\scripts\\route-check.ps1\"",
            "shell": "powershell"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "& \"${CLAUDE_PLUGIN_ROOT}\\scripts\\spawn-gate.ps1\"",
            "shell": "powershell"
          }
        ]
      }
    ]
  }
}
